# File Organizer

A simple Bash script to organize files in a directory by their file extension.

## Features
- Automatically detects file extensions.
- Creates folders for each extension type.
- Moves files into corresponding folders.

## Installation
Clone the repository:
```bash
git clone https://github.com/Whitedevil9103/file-organizer.git
cd file-organizer

